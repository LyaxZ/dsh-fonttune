# 更新日志

**dsh-fonttune** 的重要变更都记录在这里。英文版见 [CHANGELOG.en.md](CHANGELOG.en.md)。

## [0.2.5] - 2026-09-23

### 修复
- **以「链接到源码目录」的方式安装时，0.1.5-rc.x 上的配置卡片与字体一起消失**：插件会用自己目录里的 schema 库副本，而不是 DSH 自带的那一份；副本的较新版本给配置加了 rc 线宿主处理不了的标记，设置命名空间因此根本没有注册出去——设置页看不到卡片、注入的样式是空的、字体毫无变化。现在插件始终使用 DSH 自带的那一份，链接安装与正常安装在行为上完全一致。

## [0.2.4] - 2026-09-23

### 新增
- **同一个安装包同时支持两条宿主线**：DSH **0.1.5-rc.1 – rc.3** 与 **0.1.7-alpha.1 – alpha.2** 上都能直接安装使用，不需要挑版本。插件在运行时按宿主实际提供的那一套设置接口工作（rc 线一套、alpha 线一套），两条都不在时只注入字体、不显示卡片。
- **alpha 上的配置入口**：在 0.1.7-alpha.x 上，卡片出现在侧栏「插件」页的插件条目「字体增强」里；rc.x 上仍在 设置 → 插件 → 插件配置。

### 修复
- **alpha 上卡片第一次打开是一排空控件**：此前读到的是宿主的内部引用而不是配置值，注入的样式因此为空；现在首帧就是正确的字体与读数。
- **alpha 上改设置提示「设置没有保存成功」**：插件有几率把改动写到不属于自己的位置；现在只在确认是自己的配置之后才写入。

### 变更
- 兼容声明与中英 README 的兼容表更新到 0.2.4，逐条列出五条实测过的宿主线（0.1.5-rc.1 / rc.2 / rc.3、0.1.7-alpha.1 / alpha.2），并写清两条线各自的设置入口。

## [0.2.3] - 2026-09-22

### 修复
- **改设置失败不再显得像成功了**：保存被宿主拒绝时，预设行右侧会出现一句「设置没有保存成功：原因」，不再只是把控件弹回旧值让人以为存上了。
- **没有剪贴板的环境里导出也能拿到完整内容**：优先用剪贴板；远程 HTTP 入口取不到剪贴板时退回旧的复制通道；两条都不行才把完整 JSON 放进一个已全选的只读框，按一次复制快捷键即可。
- **选字体面板**：内置的等宽 / 中文 / 拉丁 / 通用四组不再因为本机字体枚举成功而消失（本机没装的预设字体也能按名字搜到）；同一个字体只列一次；拒绝了字体权限的浏览器不再被永久记住，二十秒后再打开会重试。
- 每一条按主题生成的规则都带上了主题前缀（逐主题目前还没开放，属提前修掉的隐患）。

### 变更
- **注入的样式更小、后台更省电**：排除列表的写法收敛后，一份真实配置的样式表从 2735 字节降到 2440 字节；界面字号轴的令牌轮询改为先比三个廉价信号，环境没变就完全不读样式表。
- 兼容表补 0.2.3。


## [0.2.2] - 2026-09-16

### 修复
- **简单模式下「中文字体」选不了也清不掉**：点中文格子选字体、或点中文 chip 的 ×，会直接报错且改动丢失（西文格正常）；现在三个字体分区的中文槽都能正常写入。
- **`MS PGothic` / `MS PMincho` 也被认作中文字体**：此前会被判成西文，影响简单模式中文槽的推导。
- **补上「控件没接线」这类漏洞的护栏**：离线检查现在会把卡片上每个控件的回调都真点一遍（滑块、分段控件、字体选择器、预设下拉、文本输入、每个按钮），漏接会立刻失败。

### 变更
- **发布不再可能发出未测试的包**：打标签时先跑一遍完整检查再打包，同时校验打包产物与源码一致、标签与版本号一致。
- 文档纠错：改完源码必须先构建（浏览器加载的是构建产物）；市场条目只是**声明**兼容信息，并不存在安装前的自动预检。


## [0.2.1] - 2026-09-16

### 修复
- **关掉「界面跟随对话」后界面字重不回落、重置也没用**：页面里此前同时存在两份注入样式，随首页下发的那份只在下次刷新时重建，所以「删掉一条规则」这类改动永远不生效；现在页面里始终只有一份，关掉跟随或重置都会立刻生效。
- **从跟随切到不跟随，界面不再跳回旧值**：关闭的那一刻把界面当时正在显示的值存成它自己的值，视觉上零变化、滑块也从当前位置继续调；对话没设的那一项保持 DSH 原样。
- **界面字重回来了，而且真的碰不到对话**：把对话正文与代码整棵子树从界面字重规则里排除掉，实测界面文字全部生效、对话一个字都没被带动。
- **对话分区里「未设置时跟随界面」的说明全部改成如实描述**（跟随关系一直是单向的：界面跟随对话）。
- 卡片中的预览不再被界面字重波及：对话、代码、界面三块预览各自对应真实效果。

### 变更
- **「跟随对话」开关现在同时管字体与字重**：开启时界面沿用对话的字体与字重（界面自己的两个控件隐藏），关闭后界面用自己的两项，两边互不影响。


## [0.2.0] - 2026-09-16

### 新增
- **排印分成三套**：**对话**（正文、表格、标题，字体 / 字号 / 行高 / 字重全都有）、**界面**（字体一项，默认跟随对话）、**代码**（独立一套，含连字与特性开关）。
- **对话字号**：在 DSH 自己的对话字号之上 −3 ~ +6 px，段落、标题与行高一起跟随。
- **对话行高**：100% ~ 160%；代码行高是独立的加减偏移。
- **代码连字**：默认 / 开启 / 关闭三档，高级模式还可填 `font-feature-settings`。
- **拒绝合成斜体与合成粗体**：简单模式一个开关同时禁掉，高级模式分开控制。
- **预设方案**：卡片里的下拉框，内置五个默认方案，点选即应用；选中之后所有改动自动存入该方案；右侧可改名、导入、导出（导入按名合并，最多 20 个，跨浏览器同步）。
- **卡片改版**：编辑模式 + 预设条 + 对话 / 界面 / 代码三个可折叠分区，默认全部收起、一次只展开一个，展开后末尾是该分区的独立预览；行高与控件形状与 dsh-quick-toc 对齐。

### 变更
- **界面的字号、行高、字重三项退役**：DSH 没有界面尺寸接口（设置页与侧栏的字号是上游写死的），此前这几轴既够不到界面、又会误动对话；现在字号 / 行高 / 字重归对话独占，界面只保留实测生效的字体。老配置里的这些字段继续保留、照常解析。
- 字体族改由官方主题层下发，主题切换后立刻生效。
- 深浅色本版共用同一套数值，按主题分别设置留到以后。

### 修复
- **「改界面，动的却是对话」**：界面字号轴此前会连带缩放到对话的字号链，现已隔离。
- **对话字号偏移只影响标题、不影响段落**：改为在源头加一次偏移，段落、标题与行高一起跟随。
- **暗色主题下深色的值会被浅色的值盖掉**。
- **字重读数写「未设置」而滑块停在 400**：现在读数就是 400。
- **行高滑块拖动时 `%` 会消失**：拖动过程中同样显示单位。
- **对话分区的标签显示成英文变量名**：中英文案补齐。
- 展开后首行离标题太远、展开态箭头不在最右。


## [0.1.6] - 2026-09-15

### 修复
- **配置卡片的展开箭头与宿主不一致**：原先是文本字符，比宿主卡片的箭头更细、旋转时还会偏移；现在换成与宿主同款的内联箭头图标，绕自身中心翻转。


## [0.1.5] - 2026-09-15

### 新增
- **代码字重**：字重拆成「正文字重」与「代码字重」两根互不影响的滑块（各 300 ~ 600，400 表示不动），调正文不再带动代码。

### 变更
- 卡片里字重滑块移进对应的字体分组，两处文案分开。
- 未设置代码字重时显式写回 DSH 原值，旧配置升级后代码不会跟着正文一起变粗。


## [0.1.4] - 2026-09-15

### 新增
- **代码字号**：字号拆成「正文字号」与「代码字号」两根互不影响的滑块（各 −3 ~ +6 px，0 表示不动），调正文不再带动代码。

### 变更
- 卡片里字号滑块移到对应字体下面，两处滑块文案分开；旧配置升级后正文行为不变、代码回到 DSH 原值。


## [0.1.3] - 2026-09-14

### 变更
- 开关行左侧说明改为「编辑模式」，右侧两个选项恢复为一个外框、中间一条分隔线的连体分段样式。
- 卡片描述补上「中西文分栏」。
- README 精简：去掉「松手才生效」「首帧不闪字体」「中英双语」三条实现细节（行为未变）。


## [0.1.2] - 2026-09-14

### 变更
- README 重写为「功能 / 安装 / 使用 / 兼容性 / 开发」，设计取舍与开发期踩坑不再写进 README（中英两份同步）。
- 兼容声明收紧：只列 0.1.5 线的最新补丁——同一大版本内的旧版本按语义化版本约定向上兼容，不再逐个罗列。
- npm 描述精简（已发布版本的元数据无法修改，故从 0.1.2 起生效）；英文卡片标题改为「Font tune」，与包名一致。


## [0.1.1] - 2026-09-14

### 新增
- 英文更新日志 `CHANGELOG.en.md`，随包发布。
- 打完 `v*` 标签自动打包并创建 / 更新 Release，另外产出一个不带版本号的 `dsh-fonttune.tgz`（供市场条目长期引用）；Release 正文取对应版本的中文段落，英文折叠在里面。


## [0.1.0] - 2026-09-14

首个版本：宿主半 + 客户端半的双半插件。客户端半负责设置卡片与样式注入，宿主半注册设置命名空间并把已保存的声明随首页下发，因此首帧就是保存的字体。

### 新增
- **插件配置卡片**：出现在 设置 → 插件 → 插件配置，读写 DSH 自己的设置文档。
- **正文与代码两套字体族**：各一串回退列表，留空即完全不动 DSH 的字体。
- **选字体面板**：内置等宽 / 中文 / 拉丁 / 通用四组，Chromium 下可列出本机已安装字体，搜不到的名字可新建，每行用自身字体渲染。
- **中西分家（简单模式）**：把字体栈拆成「西文字体」「中文字体」两个格子，其余回退项与顺序原样保留；高级模式是完整的 chip 编辑器，支持拖拽排序与实时预览。
- **全局字号偏移**（−3 ~ +6 px，与 DSH 自己的字号滑块叠加）与**全局字重**（300 ~ 600，400 表示不动）。
- **滑块松手才写入**：拖动过程零写入，松手后读数保持到宿主确认，不再回弹。
- **中英双语文案**；字体名经过消毒，无法闭合声明或另起规则。

### 修复
- **卡片首次渲染就崩溃**（发布阻断）：订阅方法被裸传导致 `this` 丢失，卡片在插件配置页里根本不出现。
- **字号偏移的三个问题**：body 自身没被缩放、刷新时把插件自己的声明当成基准越乘越大、派生字号被重复缩放。
- **字体族铺不进对话区与侧栏**（那些区域自己声明了字体变量）。
- **字重被吸附到整百档**：现在按所选整数原样生效。
- **字体名消毒过于宽松**。

